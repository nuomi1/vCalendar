# 任务清单：合并 ics-calendar-export 和 ipo-calendar-event-generation 规格

这是一个规格文档合并变更，不涉及代码修改。

## 1. 归档被合并的规格

- [x] 1.1 将 `openspec/specs/ipo-calendar-event-generation/` 移动至 archive 目录

## 2. 验证主规格完整性

- [x] 2.1 确认 `openspec/specs/ics-calendar-export/spec.md` 包含所有有效需求
- [x] 2.2 运行 `openspec verify` 确保规格有效

## 3. 完成变更

- [x] 3.1 归档变更 `merge-ics-export-specs` 至 archive